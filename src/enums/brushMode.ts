enum brushMode {
  // PartyMode: randomize the hue
  PartyMode,
  // SingleColor: the selected color only
  SingleColor,
  // IncreasingOrDecreasingHue increases hue overtime
  IncreasingOrDecreasingHue,
  // ShadesMode: Randomizes Lightness value (you can modify the range)
  ShadesMode
}
